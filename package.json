{
  "name": "bdi-fantasy-hq",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "test": "node --test scripts/selftest.mjs && node --test --test-force-exit --test-concurrency=6 scripts/smoketest.mjs",
    "snapshot": "node scripts/fetch-snapshot.mjs",
    "probe": "node scripts/fetch-snapshot.mjs --probe"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}